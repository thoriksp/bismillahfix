# Budget Tracker Firebase
A simple Vite + React budget tracker app using Firebase Realtime Database and Firebase Authentication.

## How to use
1. Copy `.env.example` to `.env` and fill with your Firebase config (see DEPLOYMENT_GUIDE.md).
2. Install dependencies: `npm install`
3. Run dev server: `npm run dev`
4. Build: `npm run build`

